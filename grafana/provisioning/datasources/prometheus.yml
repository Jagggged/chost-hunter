# Grafana 자동 프로비저닝 - Prometheus 데이터소스
# Grafana가 처음 뜰 때 이 파일을 읽어 데이터소스를 자동 등록한다.
# 수동으로 UI에서 추가할 필요 없음.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
